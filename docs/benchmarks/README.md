# Milestone 2 Benchmarks

`milestone-2-live-benchmark.json` is generated by:

```bash
/Users/b4iterdev/.local/bin/uv run python scripts/run_milestone2_benchmark.py --output docs/benchmarks/milestone-2-live-benchmark.json
```

The benchmark is bounded to 12 total public HTTPS requests, uses no credentials, cookies, browser automation, bypass, submissions, or invented payloads, and records blocked or policy-unverified sources as zero live extraction.
