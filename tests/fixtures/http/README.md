These synthetic HTTP fixtures are provenance-labeled milestone 2 tests. They are not live results.

They model only locally observed/authorized shapes from `.hermes/plans/milestone-2-http-ingestion.md`:

- VietnamWorks fixture JSON includes the previously observed `jobDescription`, `jobRequirement`, and absolute `jobUrl` fields.
- ITviec fixture HTML models server-rendered search/detail DOM cards and same-host detail links.

Live extraction evidence must be stored separately under `docs/benchmarks/` and never inferred from these fixtures.
